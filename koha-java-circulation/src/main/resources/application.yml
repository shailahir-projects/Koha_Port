server:
  port: 8084
spring:
  liquibase:
    enabled: false
  application:
    name: koha-java-circulation
  datasource:
    url:     username:     password: management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
