server:
  port: 8082
spring:
  liquibase:
    enabled: false
  application:
    name: koha-java-patron
  datasource:
    url:     username:     password: management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
