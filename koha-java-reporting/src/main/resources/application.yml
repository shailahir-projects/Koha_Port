server:
  port: 8088
spring:
  liquibase:
    enabled: false
  application:
    name: koha-java-reporting
  datasource:
    url:     username:     password: management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
