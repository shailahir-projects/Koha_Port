server:
  port: 8089
spring:
  liquibase:
    enabled: false
  application:
    name: koha-java-ill
  datasource:
    url:     username:     password: management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
