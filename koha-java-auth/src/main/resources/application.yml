server:
  port: 8095
spring:
  liquibase:
    enabled: false
  application:
    name: koha-java-auth
  datasource:
    url:     username:     password: management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
