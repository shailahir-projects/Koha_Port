server:
  port: 8081
spring:
  liquibase:
    enabled: false
  application:
    name: koha-java-acquisitions
  datasource:
    url:     username:     password: management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
