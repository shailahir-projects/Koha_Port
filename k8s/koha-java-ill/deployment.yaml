apiVersion: apps/v1
kind: Deployment
metadata:
  name: koha-java-ill
  namespace: koha
spec:
  replicas: 2
  selector:
    matchLabels:
      app: koha-java-ill
  template:
    metadata:
      labels:
        app: koha-java-ill
    spec:
      containers:
        - name: koha-java-ill
          image: shailahir/koha-java-ill:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: koha-java-ill-config
          ports:
            - containerPort: 8080
          resources:
            limits:
              memory: "512Mi"
              cpu: "500m"
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 10
