apiVersion: apps/v1
kind: Deployment
metadata:
  name: koha-java-erm
  namespace: koha
spec:
  replicas: 2
  selector:
    matchLabels:
      app: koha-java-erm
  template:
    metadata:
      labels:
        app: koha-java-erm
    spec:
      containers:
        - name: koha-java-erm
          image: shailahir/koha-java-erm:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: koha-java-erm-config
          ports:
            - containerPort: 8080
          resources:
            limits:
              memory: "512Mi"
              cpu: "500m"
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 10
