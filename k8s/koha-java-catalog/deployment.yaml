apiVersion: apps/v1
kind: Deployment
metadata:
  name: koha-java-catalog
  namespace: koha
spec:
  replicas: 2
  selector:
    matchLabels:
      app: koha-java-catalog
  template:
    metadata:
      labels:
        app: koha-java-catalog
    spec:
      containers:
        - name: koha-java-catalog
          image: shailahir/koha-java-catalog:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: koha-java-catalog-config
          ports:
            - containerPort: 8080
          resources:
            limits:
              memory: "512Mi"
              cpu: "500m"
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 10
