apiVersion: apps/v1
kind: Deployment
metadata:
  name: koha-java-notification
  namespace: koha
spec:
  replicas: 2
  selector:
    matchLabels:
      app: koha-java-notification
  template:
    metadata:
      labels:
        app: koha-java-notification
    spec:
      containers:
        - name: koha-java-notification
          image: shailahir/koha-java-notification:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: koha-java-notification-config
          ports:
            - containerPort: 8080
          resources:
            limits:
              memory: "512Mi"
              cpu: "500m"
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 10
