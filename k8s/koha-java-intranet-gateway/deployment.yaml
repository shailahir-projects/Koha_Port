apiVersion: apps/v1
kind: Deployment
metadata:
  name: koha-java-intranet-gateway
  namespace: koha
spec:
  replicas: 2
  selector:
    matchLabels:
      app: koha-java-intranet-gateway
  template:
    metadata:
      labels:
        app: koha-java-intranet-gateway
    spec:
      containers:
        - name: koha-java-intranet-gateway
          image: shailahir/koha-java-intranet-gateway:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: koha-java-intranet-gateway-config
          ports:
            - containerPort: 8080
          resources:
            limits:
              memory: "512Mi"
              cpu: "500m"
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 10
