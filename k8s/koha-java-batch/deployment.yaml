apiVersion: apps/v1
kind: Deployment
metadata:
  name: koha-java-batch
  namespace: koha
spec:
  replicas: 2
  selector:
    matchLabels:
      app: koha-java-batch
  template:
    metadata:
      labels:
        app: koha-java-batch
    spec:
      containers:
        - name: koha-java-batch
          image: shailahir/koha-java-batch:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: koha-java-batch-config
          ports:
            - containerPort: 8080
          resources:
            limits:
              memory: "512Mi"
              cpu: "500m"
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 10
