apiVersion: apps/v1
kind: Deployment
metadata:
  name: koha-java-auth
  namespace: koha
spec:
  replicas: 2
  selector:
    matchLabels:
      app: koha-java-auth
  template:
    metadata:
      labels:
        app: koha-java-auth
    spec:
      containers:
        - name: koha-java-auth
          image: shailahir/koha-java-auth:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: koha-java-auth-config
          ports:
            - containerPort: 8080
          resources:
            limits:
              memory: "512Mi"
              cpu: "500m"
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 10
