server:
  port: 8085
spring:
  liquibase:
    enabled: false
  application:
    name: koha-java-holds
  datasource:
    url:     username:     password: management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
