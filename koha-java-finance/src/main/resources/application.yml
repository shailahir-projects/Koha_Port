server:
  port: 8087
spring:
  liquibase:
    enabled: false
  application:
    name: koha-java-finance
  datasource:
    url:     username:     password: management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
