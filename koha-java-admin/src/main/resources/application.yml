server:
  port: 8083
spring:
  liquibase:
    enabled: false
  application:
    name: koha-java-admin
  datasource:
    url:     username:     password: management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
