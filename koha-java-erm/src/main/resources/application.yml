server:
  port: 8093
spring:
  liquibase:
    enabled: false
  application:
    name: koha-java-erm
  datasource:
    url:     username:     password: management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
